apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  # Selector currently points to blue
  # All traffic goes to blue-deployment Pods
  selector:
    app: my-app
    version: blue
  ports:
    - port: 80
      targetPort: 80
