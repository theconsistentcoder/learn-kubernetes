apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  # Selector only matches "app: my-app"
  # Does not care about track — routes to both stable and canary Pods
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: 80
