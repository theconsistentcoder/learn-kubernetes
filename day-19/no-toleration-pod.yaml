apiVersion: v1
kind: Pod
metadata:
  name: my-no-toleration-pod
  labels:
    app: my-app
spec:
  # No toleration — this Pod will be blocked by the taint
  # STATUS will show Pending forever
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80
