apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  # clusterIP: None makes this a headless Service
  # Required for StatefulSet stable DNS per Pod
  clusterIP: None
  selector:
    app: my-statefulset
  ports:
    - port: 80
      targetPort: 80
